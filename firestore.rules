rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Global default-deny catch-all
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isStaff() {
      return isSignedIn() && (
        request.auth.token.email == "joaquinvillanuevavarela@gmail.com" ||
        request.auth.token.email == "valentina@gmail.com" ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
         (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin" ||
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "instructor"))
      );
    }

    function isProfileEmail(userEmail) {
      return exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.email == userEmail;
    }

    function isAdmin() {
      return isSignedIn() && (
        request.auth.token.email == "joaquinvillanuevavarela@gmail.com" ||
        request.auth.token.email == "valentina@gmail.com" ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin")
      );
    }

    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    function incoming() {
      return request.resource.data;
    }

    function existing() {
      return resource.data;
    }

    // --- Users Collection ---
    match /users/{userId} {
      // Allow read, create, and update for anonymous lookup and simulated login flows
      allow read, create, update: if true;
      allow delete: if isSignedIn() && (
        request.auth.token.email == "joaquinvillanuevavarela@gmail.com" || 
        request.auth.token.email == "valentina@gmail.com" ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin")
      );
    }

    // --- Packages Collection ---
    match /packages/{packageId} {
      allow read: if isSignedIn() && (resource.data.studentId == request.auth.uid || isProfileEmail(resource.data.studentEmail) || isStaff());
      allow create: if isSignedIn() && (incoming().studentId == request.auth.uid || isProfileEmail(incoming().studentEmail) || isStaff());
      allow update: if isSignedIn() && (resource.data.studentId == request.auth.uid || isProfileEmail(resource.data.studentEmail) || isStaff());
      allow delete: if isAdmin();
    }

    // --- Classes Collection ---
    match /classes/{classId} {
      allow read: if isSignedIn();
      allow write: if isStaff();
    }

    // --- Bookings Collection ---
    match /bookings/{bookingId} {
      allow read: if isSignedIn() && (resource.data.studentId == request.auth.uid || isProfileEmail(resource.data.studentEmail) || isStaff());
      allow create: if isSignedIn() && (incoming().studentId == request.auth.uid || isProfileEmail(incoming().studentEmail) || isStaff());
      allow update: if isSignedIn() && (resource.data.studentId == request.auth.uid || isProfileEmail(resource.data.studentEmail) || isStaff());
      allow delete: if isStaff();
    }

    // --- Expenses Collection ---
    match /expenses/{expenseId} {
      allow read, write: if isAdmin();
    }
  }
}
